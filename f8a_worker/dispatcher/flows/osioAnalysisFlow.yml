---
  flow-definitions:
    - name: 'osioAnalysisFlow'
      queue: '{DEPLOYMENT_PREFIX}_api_osioAnalysisFlow_v0'
      edges:
        - from:
          to: 'DependencyParserTask'
        - from: 'DependencyParserTask'
          to: 'UnknownDependencyFetcherTask'
        - from: 'UnknownDependencyFetcherTask'
          to: 'bayesianApiFlow'
          foreach:
            import: 'f8a_worker.dispatcher.foreach'
            function: 'iter_unknown_dependencies'
            # result of the function is used as sub-flow arguments
            propagate_result: true
            # condition:
            #   and:
                    # - <<: *recursiveAnalysisCheck
                    #      - name: 'envEqual'
                    # args:
                    #env: 'WORKER_ADMINISTRATION_REGION'
                    #value: 'api'
        #- from: 'bayesianApiFlow'
        #  to: 'OSIOReportGenerationTask'
        #- from: 'OSIOReportGenerationTask'
        #  to: 'OSIOEmailNotificationTask'

