---
  # This flow is for the git operations to generate the manifest files for the stack analysis.
  flow-definitions:
    - name: 'gitOperationsFlow'
      queue: '{DEPLOYMENT_PREFIX}_ingestion_gitOperationsFlow_v0'
      edges:
        - from:
          to: 'GitOperationTask'